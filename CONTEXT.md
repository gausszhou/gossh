# gossh 会话管理器

一个基于 Go 技术栈的 SSH 客户端：本地运行 Go 服务端，浏览器（Vue3 + xterm.js）作为客户端 UI。用户维护主机清单，在浏览器中建立 SSH 会话、以页签管理多个并行会话；支持凭据管理、单命令执行、文件传输、端口转发与跳板机连接。工程底座整体搬迁自同为 MIT 的 gotty 项目（`internal/api → internal/session → internal/terminal` 分层与前端页签模型），本上下文覆盖 gossh 独有的领域：SSH 会话、主机、凭据、连接链。

## 语言

**会话管理器（Session Manager）**:
本产品的定位——以浏览器为 UI 管理主机清单并建立 SSH 会话的客户端。
_避免使用_: Web 终端、网关

**主机（Host）**:
用户维护的服务器条目，携带连接地址、端口、用户名、凭据引用与分组归属等建连所需信息。
_避免使用_: 服务器、节点、机器

**会话（Session）**:
与某台主机建立的一次可交互连接，占用一个 PTY，可打开、关闭、切换。
_避免使用_: 连接（Connection，指底层传输层对象）、窗口

**凭据（Credential）**:
身份验证信息来源的统称——私钥文件路径引用、SSH 代理、或密码。密码与加密私钥的口令经系统 keyring（Linux Secret Service / macOS Keychain / Windows Credential Manager）加密保存；无 keyring 守护进程时回退为仅内存保存，连接时在浏览器输入。
_避免使用_: 密码、认证方式

**主机清单（Host Inventory）**:
服务端持久化于 `~/.gossh/hosts.json` 的主机记录集合，是连接配置的唯一事实来源。浏览器只是清单的视图。
_避免使用_: 服务器列表、清单（清单特指 gotty 的浏览器侧 Manifest）

**分组（Group）**:
主机清单的组织单元，用于分类与批量操作。
_避免使用_: 文件夹、目录、集合

**跳板机（Jump Host）**:
连接链中位于客户端与目标主机之间的中间主机，连接经由它转发。
_避免使用_: 网关（指网络层概念，非主机）

**连接链（Connection Chain）**:
从本机到目标主机的跳板序列，由主机记录中的 via 引用表达，可递归嵌套至任意深度（ProxyJump 语义）；SFTP 与端口转发共用同一条链。
_避免使用_: 跳板、链路（链路指网络层）

**访问令牌（Access Token）**:
服务启动时生成的随机令牌，浏览器访问前必须出示；与 ws-origin 校验共同构成访问控制。默认仅监听 127.0.0.1。
_避免使用_: 密码、登录口令

**主机密钥指纹（Host Key Fingerprint）**:
首连时记录到 `~/.gossh/known_hosts` 的信任凭证（TOFU）；后续指纹不匹配即拒绝连接并告警。与 OpenSSH 的 known_hosts 互不通用。
_避免使用_: 证书、指纹库