# GoSSH 会话管理器

一个基于 Go 技术栈的 SSH 客户端：本地运行 Go 服务端，浏览器（Vue3 + xterm.js）作为客户端 UI。用户维护主机清单，在浏览器中建立 SSH 会话、以页签管理多个并行会话；支持凭据管理、文件传输、端口转发。工程底座整体搬迁自同为 MIT 的 gotty 项目（`internal/api → internal/session → internal/terminal` 分层与前端页签模型），本上下文覆盖 gossh 独有的领域：SSH 会话、主机、凭据、端口转发。

## 语言

**会话管理器（Session Manager）**:
本产品的定位——以浏览器为 UI 管理主机清单并建立 SSH 会话的客户端。
_避免使用_: Web 终端、网关

**主机（Host）**:
用户维护的服务器条目，携带连接地址、端口、用户名、凭据引用等建连所需信息。
_避免使用_: 服务器、节点、机器

**会话（Session）**:
与某台主机建立的一次可交互连接，占用一个 PTY，可打开、关闭、切换。
_避免使用_: 连接（Connection，指底层传输层对象）、窗口

**凭据（Credential）**:
身份验证信息来源的统称——私钥文件路径引用、SSH 代理、或密码。密码与加密私钥的口令经系统 keyring（Linux Secret Service / macOS Keychain / Windows Credential Manager）加密保存；无 keyring 守护进程时回退为仅内存保存，连接时在浏览器输入。
_避免使用_: 密码、认证方式

**主机清单（Host Inventory）**:
服务端持久化于 `~/.gossh/hosts.json` 的主机记录集合，是连接配置的唯一事实来源。浏览器只是清单的视图。
_避免使用_: 服务器列表、清单（清单特指 gotty 的浏览器侧 Manifest）

**端口转发（Port Forward）**:
把一条本地或远端端口与远端目标之间的通路映射到 SSH 连接上。分两级：主机级端口转发写在主机记录上，运行在主机专属的转发连接上，不随任何会话生灭（会话建立时确保其就绪，多数情况下无需先建会话；仅存在于浏览器的密码在其首次会话建立时顺带成为转发连接的拨号凭据）；会话级端口转发在会话内临时添加，仅当次有效。断线不自动重连（状态可见，下次会话或配置变更重拨）。
_避免使用_: 隧道（Tunnel，指 VPN 等网络层概念）

**访问令牌（Access Token）**:
服务启动时生成的随机令牌，浏览器访问前必须出示；与 ws-origin 校验共同构成访问控制。默认仅监听 127.0.0.1。
_避免使用_: 密码、登录口令

**主机密钥指纹（Host Key Fingerprint）**:
首连时记录到 `~/.gossh/known_hosts` 的信任凭证（TOFU）；后续指纹不匹配即拒绝连接并告警。与 OpenSSH 的 known_hosts 互不通用。
_避免使用_: 证书、指纹库

**桌面形态（Desktop App）**:
`gossh app` 子命令提供的系统集成式使用方式——同一进程内启动服务端，托盘常驻（打开界面 / 开机自启开关 / 退出），自动打开浏览器并注入访问令牌，单实例保护（Linux 为 flock 锁文件，Windows 为命名互斥体）。UI 仍是浏览器，不是独立桌面窗口。Linux 的托盘需要 cgo 构建；Windows 托盘是纯 Go 实现，默认 release 二进制即可用。
_避免使用_: 客户端、客户端软件（泛指浏览器 UI 本身）