# 功能裁剪记录

本仓库按产品决策逐步删除/收窄功能。此文件记录「砍了什么、什么时候砍的、
为什么、能否恢复」,避免后续误以为功能丢失是缺陷。

## 记录

| 功能 | 状态 | 版本(提交) | 日期 | 说明 |
|---|---|---|---|---|
| 单命令执行(run) | **已删除** | v0.1.1(`5e9137d`) | 2026-09-05 | `gossh run` 子命令、`POST /api/run`、前端 RunModal/RunView/运行页签全部移除,共删约 1009 行。理由:暂不需要。恢复需重写(无保留代码)。 |
| SFTP 文件传输 | **已删除** | 未发布(本轮) | 2026-09-11 | 前端面板早先已在 `ef2e320` 删除,本轮把后端与编译开关一并清干净:删 `internal/api/sftp_{handler,routes,routes_disabled}.go` 及 `/api/**/sftp/*` 七个端点、移除 `pkg/sftp`(含 `kr/fs`)依赖、Makefile `SFTP=1`/`VITE_ENABLE_SFTP` 开关、CI `-tags sftp` 编译检查、smoke 第 4 步与前端残留 `sftp.*` i18n 键。理由:暂不需要,保持二进制精简。恢复需重写(无保留代码)。 |
| 跳板机(via) | **已删除** | v0.1.2(`0a64d27`) | 2026-09-05 | 主机记录 `via` 字段、连接链解析(`Chain`/`Parents`)、多跳 ProxyJump 拨号(`dialVia`)、`GET /api/hosts/{id}/parents`、前端跳板下拉/via 路径展示、CLI `--via` 全部移除。理由:暂不需要,拨号路径简化为单跳直连。恢复需重写。 |
| 分组与搜索 | **已删除** | v0.1.3(本轮) | 2026-09-05 | 主机记录 `group` 字段、CLI `--group`、前端分组折叠/未分组栏与搜索框全部移除,主机列表回归扁平单列。理由:暂不需要。恢复需重写。 |

## 说明

- 删除类功能(上表「已删除」)不在代码库保留实现,恢复意味着重新开发;
- 每次裁剪同步更新 `CONTEXT.md`(领域术语)、`README`(功能与用法)与
  smoke 测试,保证文档与实现一致。